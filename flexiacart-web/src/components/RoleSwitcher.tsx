"use client";

import React from 'react';
import { useAuth } from '@/context/AuthContext';

export default function RoleSwitcher() {
  // Harmless component that returns null in production so it never breaks the build or shows on UI
  return null;
}
